
<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <h1 class="text-center my-4">New Review for <%= @restaurant.name %></h1>
     <%= render "reviews/form", reviews: @review %>
      <div class="text-center mt-3">
        <%= link_to 'Back to Restaurant', restaurant_path(@restaurant), class: 'btn btn-secondary' %>
      </div>
    </div>
  </div>
</div>
